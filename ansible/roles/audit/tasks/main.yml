---
- name: Assert the operating-system baseline
  ansible.builtin.assert:
    that:
      - ansible_facts.distribution == audit_expected_distribution
      - ansible_facts.kernel == audit_expected_running_kernel
    fail_msg: >-
      Phase 0 stop gate: observed OS or running kernel differs from the approved baseline recorded at
      {{ audit_baseline_observed_at }}.

- name: Gather installed package facts
  ansible.builtin.package_facts:
    manager: auto

- name: Assert baseline package versions
  ansible.builtin.assert:
    that:
      - item.key in ansible_facts.packages
      - item.value in (ansible_facts.packages[item.key] | map(attribute='version') | list)
    fail_msg: "Phase 0 stop gate: package {{ item.key }} differs from {{ item.value }}."
  loop: "{{ audit_expected_packages | dict2items }}"
  loop_control:
    label: "{{ item.key }}"

- name: Assert deferred packages remain absent
  ansible.builtin.assert:
    that:
      - item not in ansible_facts.packages
    fail_msg: "Phase 0 stop gate: deferred package {{ item }} is unexpectedly installed."
  loop: "{{ audit_expected_absent_packages }}"

- name: Gather service facts
  ansible.builtin.service_facts:

- name: Assert baseline services are enabled and running
  ansible.builtin.assert:
    that:
      - item.key in ansible_facts.services
      - ansible_facts.services[item.key].state == item.value.state
      - ansible_facts.services[item.key].status == item.value.status
    fail_msg: "Phase 0 stop gate: {{ item.key }} is not enabled and running."
  loop: "{{ audit_expected_services | dict2items }}"
  loop_control:
    label: "{{ item.key }}"

- name: Inspect the host-only Compose environment file metadata
  ansible.builtin.stat:
    path: "{{ audit_env_path }}"
    get_checksum: false
    get_mime: false
  register: audit_env_stat

- name: Inspect the protected home directory metadata
  ansible.builtin.stat:
    path: "{{ audit_home_dir }}"
    get_checksum: false
    get_mime: false
  register: audit_home_stat

- name: Assert environment-file containment matches the adopted baseline
  ansible.builtin.assert:
    that:
      - audit_env_stat.stat.exists
      - audit_env_stat.stat.isreg
      - audit_env_stat.stat.mode == '0644'
      - audit_env_stat.stat.pw_name == 'docker'
      - audit_env_stat.stat.gr_name == 'docker'
      - audit_home_stat.stat.isdir
      - audit_home_stat.stat.mode == '0700'
    fail_msg: >-
      Phase 0 stop gate: .env metadata or its containing home directory changed. Contents were not read.

- name: Verify exact adopted fstab entries without changing mounts
  ansible.builtin.command:
    argv:
      - /usr/bin/grep
      - --fixed-strings
      - --line-regexp
      - --
      - "{{ item }}"
      - /etc/fstab
  loop: "{{ audit_expected_fstab_entries }}"
  loop_control:
    label: "{{ item.split()[1] }}"
  changed_when: false
  check_mode: false

- name: Inspect active mount source and filesystem type
  ansible.builtin.command:
    argv:
      - /usr/bin/findmnt
      - --noheadings
      - --output
      - SOURCE,FSTYPE
      - --target
      - "{{ item.path }}"
  register: audit_mount_results
  loop: "{{ audit_expected_mounts }}"
  loop_control:
    label: "{{ item.path }}"
  changed_when: false
  check_mode: false

- name: Assert required mounts match the adopted runtime baseline
  ansible.builtin.assert:
    that:
      - item.rc == 0
      - item.stdout.split() | first == item.item.source
      - item.stdout.split() | last == item.item.fstype
    fail_msg: "Phase 0 stop gate: required mount {{ item.item.path }} differs or is unavailable."
  loop: "{{ audit_mount_results.results }}"
  loop_control:
    label: "{{ item.item.path }}"

- name: Inspect Docker client and daemon versions
  ansible.builtin.command:
    argv:
      - /usr/bin/docker
      - version
      - --format
      - "{{ audit_docker_version_format }}"
  register: audit_docker_version
  changed_when: false
  check_mode: false

- name: Inspect Docker Compose version
  ansible.builtin.command:
    argv:
      - /usr/bin/docker
      - compose
      - version
      - --short
  register: audit_compose_version
  changed_when: false
  check_mode: false

- name: Assert Docker and Compose runtime versions
  ansible.builtin.assert:
    that:
      - audit_docker_version.stdout == audit_expected_docker_client_version ~ '|' ~ audit_expected_docker_server_version
      - audit_compose_version.stdout == audit_expected_compose_version
    fail_msg: >-
      Phase 0 stop gate: Docker or Compose differs. The adopted baseline intentionally records the
      29.6.2 client/package with the still-running 29.6.1 daemon.

- name: Validate the resolved Compose model without displaying it
  ansible.builtin.command:
    argv:
      - /usr/bin/docker
      - compose
      - config
      - --quiet
    chdir: "{{ audit_compose_project_dir }}"
  changed_when: false
  check_mode: false

- name: List declared Compose services
  ansible.builtin.command:
    argv:
      - /usr/bin/docker
      - compose
      - config
      - --services
    chdir: "{{ audit_compose_project_dir }}"
  register: audit_declared_services
  changed_when: false
  check_mode: false

- name: List declared Compose volumes
  ansible.builtin.command:
    argv:
      - /usr/bin/docker
      - compose
      - config
      - --volumes
    chdir: "{{ audit_compose_project_dir }}"
  register: audit_declared_volumes
  changed_when: false
  check_mode: false

- name: List running Compose services
  ansible.builtin.command:
    argv:
      - /usr/bin/docker
      - compose
      - ps
      - --status
      - running
      - --services
    chdir: "{{ audit_compose_project_dir }}"
  register: audit_running_services
  changed_when: false
  check_mode: false

- name: List Compose container identifiers
  ansible.builtin.command:
    argv:
      - /usr/bin/docker
      - compose
      - ps
      - --quiet
    chdir: "{{ audit_compose_project_dir }}"
  register: audit_container_ids
  changed_when: false
  check_mode: false

- name: Assert Compose service and declared-volume counts
  ansible.builtin.assert:
    that:
      - audit_declared_services.stdout_lines | length == audit_expected_compose_service_count
      - audit_running_services.stdout_lines | length == audit_expected_compose_service_count
      - audit_container_ids.stdout_lines | length == audit_expected_compose_service_count
      - audit_declared_volumes.stdout_lines | length == audit_expected_declared_volume_count
    fail_msg: "Phase 0 stop gate: Compose service or declared-volume counts changed."

- name: Inspect known unhealthy containers without exposing configuration
  ansible.builtin.command:
    argv:
      - /usr/bin/docker
      - inspect
      - --format
      - "{{ audit_container_health_format }}"
      - "{{ item.key }}"
  register: audit_health_results
  loop: "{{ audit_expected_health | dict2items }}"
  loop_control:
    label: "{{ item.key }}"
  changed_when: false
  check_mode: false

- name: Assert known container health differences remain explicit
  ansible.builtin.assert:
    that:
      - item.stdout == item.item.key ~ '|running|' ~ item.item.value
    fail_msg: "Phase 0 stop gate: {{ item.item.key }} state or health differs from the recorded baseline."
  loop: "{{ audit_health_results.results }}"
  loop_control:
    label: "{{ item.item.key }}"

- name: Count Compose-project volumes without inspecting their data
  ansible.builtin.command:
    argv:
      - /usr/bin/docker
      - volume
      - ls
      - --filter
      - label=com.docker.compose.project=docker-compose
      - --quiet
  register: audit_compose_project_volumes
  changed_when: false
  check_mode: false

- name: Assert Compose-project volume count includes preserved legacy volumes
  ansible.builtin.assert:
    that:
      - audit_compose_project_volumes.stdout_lines | length == audit_expected_compose_project_volume_count
    fail_msg: "Phase 0 stop gate: the Compose-project volume count changed; do not prune or recreate volumes."

- name: Verify preserved legacy volume metadata exists
  ansible.builtin.command:
    argv:
      - /usr/bin/docker
      - volume
      - inspect
      - "{{ item.engine_name }}"
  loop: "{{ audit_legacy_volumes }}"
  loop_control:
    label: "{{ item.logical_name }}"
  changed_when: false
  check_mode: false

- name: Inspect required loaded kernel modules
  ansible.builtin.command:
    argv:
      - /usr/bin/grep
      - --extended-regexp
      - "^{{ item }} "
      - /proc/modules
  register: audit_module_results
  failed_when: false
  loop: "{{ audit_expected_modules }}"
  loop_control:
    label: "{{ item }}"
  changed_when: false
  check_mode: false

- name: Assert required kernel modules are loaded
  ansible.builtin.assert:
    that:
      - item.rc == 0
    fail_msg: "Phase 0 stop gate: kernel module {{ item.item }} is not loaded."
  loop: "{{ audit_module_results.results }}"
  loop_control:
    label: "{{ item.item }}"

- name: Inspect required host device paths
  ansible.builtin.stat:
    path: "{{ item }}"
    get_checksum: false
    get_mime: false
  register: audit_device_results
  loop: "{{ audit_expected_devices }}"
  loop_control:
    label: "{{ item }}"

- name: Assert required host device paths exist
  ansible.builtin.assert:
    that:
      - item.stat.exists
    fail_msg: "Phase 0 stop gate: required device path {{ item.item }} is missing."
  loop: "{{ audit_device_results.results }}"
  loop_control:
    label: "{{ item.item }}"

- name: Inspect running container bind and device sources
  ansible.builtin.command:
    argv: >-
      {{ ['/usr/bin/docker', 'inspect', '--format', audit_mount_device_format]
         + audit_container_ids.stdout_lines }}
  register: audit_runtime_paths
  changed_when: false
  check_mode: false

- name: Inspect each running bind or device source
  ansible.builtin.stat:
    path: "{{ item | regex_replace('^(bind|device)\\|', '') }}"
    get_checksum: false
    get_mime: false
  register: audit_runtime_path_stats
  loop: "{{ audit_runtime_paths.stdout_lines | reject('equalto', '') | unique | list }}"
  loop_control:
    label: "{{ item }}"

- name: Assert every running bind and device source still exists
  ansible.builtin.assert:
    that:
      - item.stat.exists
    fail_msg: "Phase 0 stop gate: running container source {{ item.item }} is missing."
  loop: "{{ audit_runtime_path_stats.results }}"
  loop_control:
    label: "{{ item.item }}"

- name: Compare tracked Wolf files with host copies
  ansible.builtin.command:
    argv:
      - /usr/bin/cmp
      - --silent
      - --
      - "{{ item.tracked }}"
      - "{{ item.host }}"
  register: audit_wolf_compare
  failed_when: false
  loop: "{{ audit_wolf_file_pairs }}"
  loop_control:
    label: "{{ item.host }}"
  changed_when: false
  check_mode: false

- name: Assert tracked Wolf files match host copies
  ansible.builtin.assert:
    that:
      - item.rc == 0
    fail_msg: "Phase 0 stop gate: tracked and host Wolf file differ at {{ item.item.host }}."
  loop: "{{ audit_wolf_compare.results }}"
  loop_control:
    label: "{{ item.item.host }}"

- name: Inspect DKMS status
  ansible.builtin.command:
    argv:
      - /usr/bin/dkms
      - status
  register: audit_dkms_status
  changed_when: false
  check_mode: false

- name: Assert the deferred-kernel Gasket build is installed
  ansible.builtin.assert:
    that:
      - "'gasket/' in audit_dkms_status.stdout"
      - audit_expected_dkms_kernel in audit_dkms_status.stdout
      - "'installed' in audit_dkms_status.stdout"
    fail_msg: "Phase 0 stop gate: expected Gasket DKMS build is absent. Do not automate the patched AUR package."

- name: Inspect root cron metadata without privilege or content access
  ansible.builtin.stat:
    path: "{{ audit_root_cron_path }}"
    get_checksum: false
    get_mime: false
  register: audit_root_cron_stat

- name: Assert the unadopted root crontab file still exists
  ansible.builtin.assert:
    that:
      - audit_root_cron_stat.stat.exists
    fail_msg: "Phase 0 stop gate: root cron metadata changed; inspect it with separately approved privilege."

- name: Locate the local backup archive mount without reading backup configuration
  ansible.builtin.command:
    argv:
      - /usr/bin/docker
      - inspect
      - --format
      - "{{ audit_archive_mount_format }}"
      - daily-local-backup
  register: audit_backup_archive
  changed_when: false
  check_mode: false

- name: Inspect the local backup archive directory
  ansible.builtin.stat:
    path: "{{ audit_backup_archive.stdout }}"
    get_checksum: false
    get_mime: false
  register: audit_backup_archive_stat

- name: Find one encrypted local backup without reading its contents
  ansible.builtin.command:
    argv:
      - /usr/bin/find
      - "{{ audit_backup_archive.stdout }}"
      - -maxdepth
      - "1"
      - -type
      - f
      - -name
      - "*.gpg"
      - -print
      - -quit
  register: audit_encrypted_backup
  changed_when: false
  check_mode: false
  when: audit_backup_archive_stat.stat.isdir | default(false)

- name: Assert an encrypted local backup exists
  ansible.builtin.assert:
    that:
      - audit_backup_archive_stat.stat.isdir | default(false)
      - audit_encrypted_backup.stdout | default('') | length > 0
    fail_msg: >-
      Phase 0 stop gate: no encrypted local backup was found. Restore drilling and remote-backup
      verification remain manual requirements.

- name: Simulate Compose create without builds, pulls, or changes
  ansible.builtin.command:
    argv:
      - /usr/bin/docker
      - compose
      - --dry-run
      - create
      - --no-build
    chdir: "{{ audit_compose_project_dir }}"
  changed_when: false
  check_mode: false
